{
  "name": "page-image-downloader",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check popup.js && node --check scanner.js && node --check core.js"
  }
}
